/**
 *
 * @author Kel-PC
 */
public class main {

    /**
     * @param args the command line arguments which will run. If the arguments is empty it will be run 
     * performHelp from the command prompt class
     */
    public static void main(String[] args) {
        
        CommandPrompt cmdP = new CommandPrompt(args);
        
    }
    
}
